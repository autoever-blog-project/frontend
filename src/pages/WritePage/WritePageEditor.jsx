import * as S from './WritePageStyle.js';
import { useRef } from 'react';
function WritePageEditor() {
  const editorRef = useRef();

  return <S.WritePageEditorContainer></S.WritePageEditorContainer>;
}

export default WritePageEditor;
